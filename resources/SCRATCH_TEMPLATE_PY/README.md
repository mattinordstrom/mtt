# py_scratch

## Setup

Create a virtual env:

    python3 -m venv .venv

Activate it:

    source .venv/bin/activate

Install requirements:

    pip3 install -r requirements.txt


## Run

    python3 main.py